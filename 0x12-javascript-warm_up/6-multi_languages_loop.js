#!/usr/bin/node

/* Define an array to print */
const anArray = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

/* Print each element of the array using loop */
for (let i = 0; i < anArray.length; i++) {
  console.log(anArray[i]);
}
